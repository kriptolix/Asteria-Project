site:
  title: "My Site"
  description: ""
  url: "http://localhost:8000"
  language: "en"
  home_page: "welcome"

theme:
    name: "minimal"

# Menu, navigation, social links, and other presentation settings
# are located in themes/<name>/theme.yaml (the theme's own namespace), not
# here. More options in this file (feeds, taxonomies, URLs, etc.) have
# reasonable default values ​​and don't need to be configured now. See the
# Asteria DOCUMENTATION for complete examples.
